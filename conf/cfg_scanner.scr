/**
 * File:
 *   cfg_scanner.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/scanner
 *   using the sysconfig-agent
 * Access:
 *   read/write
 * Authors:
 *   gs@suse.de
 *
 * Example:
 *
 *   Read(.sysconfig.scanner.ADJUST_SCSI_SCANNER_PERM)
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/scanner</tt>
 * in an easy manner.
 */
.sysconfig.scanner

`ag_ini(
  SysConfigFile("/etc/sysconfig/scanner")
)
